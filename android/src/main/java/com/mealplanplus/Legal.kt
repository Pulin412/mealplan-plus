package com.mealplanplus

/** Privacy-policy constants (kept in sync with the webapp's src/lib/legal.ts). */
object Legal {
    /** Recorded against the user's consent; bump when the policy text materially changes. */
    const val PRIVACY_POLICY_VERSION = "2026-07-29"

    /** Public, hosted policy — opened in the browser from onboarding + profile. */
    const val PRIVACY_POLICY_URL = "https://eatmyplan.com/privacy"
}
