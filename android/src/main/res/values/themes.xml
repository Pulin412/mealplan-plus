<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MealPlanPlus" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Branded cold-start splash (androidx core-splashscreen). Hands off to the app theme. -->
    <style name="Theme.MealPlanPlus.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.MealPlanPlus</item>
    </style>
</resources>
