spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/mealplanplus
    username: mealplan
    password: mealplan_dev
    driver-class-name: org.postgresql.Driver
    hikari:
      data-source-properties:
        stringtype: unspecified   # text -> native Postgres enum casts (see application-prod.yml)
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  h2:
    console:
      enabled: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8090
